const colors = {
  primary: "#01bcc6",
};

const styles = {
  global: {
    body: {
      bg: "#f0f4f7",
      color: "gray.700",
    },
  },
};

const fonts = {
  heading: `"Roboto", -apple-system, BlinkMacSystemFont, "Segoe UI", "Oxygen",
    "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue",
    sans-serif`,
  body: `"Roboto", -apple-system, BlinkMacSystemFont, "Segoe UI", "Oxygen",
    "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue",
    sans-serif`,
  mono: `source-code-pro, Menlo, Monaco, Consolas, "Courier New",
    monospace`,
};

const theme={ colors, styles, fonts }

export default theme;
